# S8.4 — Google Maps location and home-venue default

| | |
|---|---|
| **Epic** | 8 — Richer event model (v1.1.0) |
| **Status** | Done — built 2026-09-14, verified |
| **Depends on** | [S8.2](S8.2-match-details-and-title.md), S4.1, S4.2, S3.3 |
| **Blocks** | [S9.3](S9.3-match-share-rewrite.md) |
| **Size** | S |
| **Risk** | low — no schema change; a display refinement plus a default value |

## Goal
For an away match the manager pastes a Google Maps link as the location; for a home match the location
defaults to the club ground, **Bogies**. Players tap the location to open it in maps.

## Context
[V5](../00-decisions-v1.1.0.md): `events.location` stays free text but may hold a URL. A home match defaults
to name "Bogies" + link `https://maps.app.goo.gl/SD1NJmBYwLqz8Z7M6`. Away matches paste their own link. The
home venue lives in one `HOME_VENUE` constant.

## Scope
**In**
- `src/lib/home-venue.ts` — `HOME_VENUE = { label: 'Bogies', mapsUrl: 'https://maps.app.goo.gl/SD1NJmBYwLqz8Z7M6' }`.
- Form: when a match is set to home, the Location field defaults to the home venue (label shown, maps link
  stored); when away, the field is empty for the manager to paste a link. Manager can override either.
- A `locationDisplay(location)` helper: if the value is a URL, render a tappable link (opens in a new tab /
  the OS maps app) labelled sensibly (Bogies for the home link, "Open in Maps" otherwise); else plain text.
- Event detail, home card, manager view and the calendar day list use `locationDisplay`.

**Out**
- Any schema change (location is already text). No geocoding, no map embed — just a link (out of scope).
- The share line ("Home Game: Bogies" / "Away: …") — that is [S9.3](S9.3-match-share-rewrite.md).

## Acceptance criteria
- [x] **AC1** — Setting a match to **home** defaults the Location to the Bogies maps link (showing "Bogies"); setting **away** clears it for the manager to paste a link. The manager can override both. (`locationForMatchSide`, driven by a `homeAway`/`isMatch` effect in `EventForm`; a live "Opens as: Bogies" hint and an "away" placeholder. Unit-tested; manual-on-phone deferred.)
- [x] **AC2** — `locationDisplay` renders a `https?://` value as a tappable link (target/rel safe) and any other value as plain text; unit-tested for a maps link, a plain place name, and empty. (`LocationText` renders it with `target="_blank" rel="noopener noreferrer"`.)
- [x] **AC3** — The Bogies label and link come from the single `HOME_VENUE` constant (grep: the URL literal `SD1NJmBYwLqz8Z7M6` appears only in `src/lib/home-venue.ts`).
- [x] **AC4** — Event detail, home card, manager view (and the manager list row + cold-arrival preview) all use `locationDisplay`/`LocationText`; no ad-hoc rendering of `location`. *The calendar day list is S10.2 (not yet built) — it will use `LocationText` when built.*
- [x] **AC5** — Training/social location is a free-text field with no home default and no link behaviour unless the manager pastes a URL (in which case `locationDisplay` still links it). (The home-default effect is gated on `isMatch && homeAway === 'home'`.)

## Implementation notes
- No migration. `HOME_VENUE` constant; `locationDisplay(value): { href?: string; label: string }` in
  `src/lib/location.ts` (or `home-venue.ts`). Link opens with `rel="noopener noreferrer"`.
- Form default: on `home_away` change to 'home', if location is empty or equals the previous away value,
  set it to `HOME_VENUE.mapsUrl`. Keep it editable.
- Reuse across screens — one display helper, matching how formatEventTime is the one date formatter.

## Data and API surface
None (no schema change). New `HOME_VENUE` constant and `locationDisplay` helper.

## UI states
| Screen | Loading | Empty | Error | Populated |
|---|---|---|---|---|
| New/Edit event | n/a | n/a | inline (a home match with no location is still valid — defaulted) | Location defaults to Bogies for home; link preview |
| Event detail | skeleton | — | error card | Tappable "Bogies" / "Open in Maps" or plain text |

## Test plan
- **Unit** — locationDisplay for url/plain/empty; the home default logic on toggling home/away.
- **Integration** — none (no schema change).
- **Manual** — create a home match (location pre-filled Bogies, tappable) and an away match (paste a maps link, tappable) on a phone.

## Definition of done
- [x] All acceptance criteria met (calendar day list deferred to S10.2 — screen not yet built)
- [x] `typecheck`/`lint` clean, tests pass in CI
- [x] Four UI states handled (no schema change; location display added to the existing states)
- [x] The home-venue URL literal lives in exactly one place (`src/lib/home-venue.ts`)

## Build notes — deviations from this spec

| Spec said | Built as | Why |
|---|---|---|
| "the calendar day list" uses `locationDisplay` (AC4) | Only the three built screens wired (event detail, home card, manager view) plus the manager list row and cold-arrival preview | The calendar Home screen is S10.2, not yet built. It reuses `LocationText` when built; no ad-hoc `location` render exists to migrate. |
| `HOME_VENUE` in `home-venue.ts`, `locationDisplay` "in `location.ts` (or `home-venue.ts`)" | Both live in `src/lib/home-venue.ts` | One file, one source of truth — matches how `time.ts` is the one date formatter. |
| Home default "showing Bogies" | Store value is the maps URL; screens render "Bogies" via `locationDisplay`, and the form shows a live "Opens as: Bogies" hint under the input | `events.location` is free text holding a URL (V5); the URL is the stored value, "Bogies" is its display label. |

Manual on-phone check (create a home match, tap the pre-filled Bogies link; paste an away maps link and tap it) is deferred — verify on a real device.

## Open questions
None — the venue and link are confirmed.
