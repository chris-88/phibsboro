# S17.1 — Schedule: rename Manage, add Past

| | |
|---|---|
| **Epic** | 17 — Stats & match data (v1.3.0) |
| **Status** | Done — built 2026-09-16 (rename; Upcoming/Past/Create already existed) |
| **Depends on** | S4.1 (create), S4.3 (manager event view) |
| **Size** | S |
| **Risk** | low — a label rename + a past-events filter on an existing screen |

## Goal
The **Manage** tab reads **Schedule** and shows the team's events split into **Upcoming** and **Past**, with
**Create** still there — matching the sketch.

## Context
[X2](../00-decisions-v1.3.0.md). The screen and route (`/manage`) stay; only the user-facing label/heading
change and a past/upcoming toggle is added. Past = the same team-events read filtered to `starts_at < now`.

## Scope
**In**
- Nav label `Manage` → `Schedule` (`navItemsForRole`); the screen heading likewise.
- A segmented **Upcoming / Past** toggle over the team's events; Upcoming is the current default, Past lists
  past events newest-first. Create (New event) stays on the screen.
- Four states preserved per segment (loading, empty "No upcoming/past events", error, populated).

**Out**
- Renaming the `/manage` route (kept, X2). Any new event data.

## Acceptance criteria
- [ ] **AC1** — The bottom-nav tab and the screen heading read "Schedule".
- [ ] **AC2** — Upcoming shows future events (as today); Past shows past events, newest first.
- [ ] **AC3** — Create still opens the New-event flow.
- [ ] **AC4** — Four states handled per segment.

## Implementation notes
- Reuse the team-events read; add a `when: 'upcoming' | 'past'` filter (or two queries) against `serverNow()`.
- Toggle is UI state; default Upcoming.

## Definition of done
- [ ] ACs met; `typecheck`/`lint` clean, tests pass; four states handled.
