// Exists so the `@/` alias has something to resolve at runtime in the alias smoke test.
// Delete once a real module under @/lib is imported by a test (S1.5).
export const CLUB_NAME = 'Phibsboro FC'
