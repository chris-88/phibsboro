import { RoutePlaceholder } from '@/components/route-placeholder'

/** Placeholder. S4.1 builds this screen. */
export default function NewEventScreen(): React.JSX.Element {
  return <RoutePlaceholder story="S4.1" name="New event" />
}
