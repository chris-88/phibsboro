import { RoutePlaceholder } from '@/components/route-placeholder'

/** Placeholder. S3.1 builds this screen. */
export default function HomeScreen(): React.JSX.Element {
  return <RoutePlaceholder story="S3.1" name="Home" />
}
