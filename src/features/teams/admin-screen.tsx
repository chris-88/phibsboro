import { RoutePlaceholder } from '@/components/route-placeholder'

/** Placeholder. S6.1 builds this screen. */
export default function AdminScreen(): React.JSX.Element {
  return <RoutePlaceholder story="S6.1" name="Admin" />
}
