import { RoutePlaceholder } from '@/components/route-placeholder'

/** Placeholder. S2.1 builds this screen. */
export default function RegisterScreen(): React.JSX.Element {
  return <RoutePlaceholder story="S2.1" name="Register" />
}
