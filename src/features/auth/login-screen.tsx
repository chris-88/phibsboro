import { RoutePlaceholder } from '@/components/route-placeholder'

/** Placeholder. S2.2 builds this screen. */
export default function LoginScreen(): React.JSX.Element {
  return <RoutePlaceholder story="S2.2" name="Sign in" />
}
