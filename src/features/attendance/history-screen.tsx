import { RoutePlaceholder } from '@/components/route-placeholder'

/** Placeholder. S3.5 builds this screen. */
export default function HistoryScreen(): React.JSX.Element {
  return <RoutePlaceholder story="S3.5" name="History" />
}
